name: CD - Deploy Single Service to Kind (via SSH + SCP)

on:
  repository_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout infra repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Show deployment info
      - name: Deployment Info
        run: |
          echo "🚀 Deploying service: ${{ github.event.client_payload.service }}"
          echo "🖼️ Image: ${{ github.event.client_payload.image }}"

      # 3️⃣ Update image inside the service's deployment YAML
      - name: Update image in deployment
        run: |
          SERVICE=${{ github.event.client_payload.service }}
          IMAGE=${{ github.event.client_payload.image }}
          FILE="infra/services/${SERVICE}-deployment.yaml"
          if [ -f "$FILE" ]; then
            sed -i "s|image:.*|image: ${IMAGE}|g" $FILE
          else
            echo "❌ File not found: $FILE"
            exit 1
          fi

      # 4️⃣ Copy both deployment and service YAMLs to the server
      # 🧩 Copy secret and service YAMLs to server
      - name: Copy YAMLs to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: |
            infra/ingress/mongo-express-ingress.yaml,
            infra/secrets/mongo-secret.yaml,
            infra/services/${{ github.event.client_payload.service }}-deployment.yaml,
            infra/kub-services/${{ github.event.client_payload.service }}-service.yaml
          target: "~/deployments/"

      # 🧩 Apply manifests (Secret + Deployment + Service)
      - name: Apply manifests on server
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            SERVICE=${{ github.event.client_payload.service }}
            echo "📦 Applying secret..."
            kubectl apply -f ~/deployments/infra/secrets/mongo-secret.yaml -n default
            echo "📦 Deploying service: $SERVICE"
            kubectl apply -f ~/deployments/infra/services/${SERVICE}-deployment.yaml -n default
            kubectl apply -f ~/deployments/infra/kub-services/${SERVICE}-service.yaml -n default
               echo "📦 Applying ingres..."
            kubectl apply -f ~/deployments/infra/ingress/mongo-express-ingress.yaml -n default
            echo "⏳ Waiting for rollout..."
            kubectl rollout status deployment/${SERVICE} -n default
            echo "✅ Deployment complete for $SERVICE"
